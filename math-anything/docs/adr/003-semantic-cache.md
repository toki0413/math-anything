# ADR-003: LLM 语义缓存

## 状态
已采纳

## 上下文
LLM API 调用成本高、延迟大。相同/相似查询重复调用浪费 token。

## 决策
实现 SemanticCache：
- 语义归一化（同义词映射 + JSON key 排序 + 空白标准化）
- SHA-256 哈希缓存键
- LRU 淘汰 + TTL 过期
- 参数感知缓存（浮点数四舍五入减少噪声未命中）

## 后果
- 正面：减少重复 LLM 调用，节省 token 和延迟
- 负面：缓存一致性需维护，语义归一化可能过度合并
- 缓解：TTL 过期 + 手动 invalidate
